---
http:
  host: "0.0.0.0"
  port: 5636
  tls:
    enabled: false

authentication: false

database:
  type: sqlite

  retention:
    days: 7

input:
  enabled: true
  paths:
    - "/var/log/suricata/eve.json"
    - "/var/log/suricata/eve.*.json"
